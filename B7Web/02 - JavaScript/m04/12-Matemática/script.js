
//let novoValor = Math.round(Math.PI);  
let novoValor = Math.floor(Math.random() * 100);
    // floor() arredonda o valor para o inteiro anterior mais próximo
    // ceil() arredonda o valor para o inteiro mais próximo
    // round() arredonda o valor para o inteiro mais próximo
    // random() gera um número aleatório entre 0 e 1
    // abs() retorna o valor absoluto
console.log(novoValor);
